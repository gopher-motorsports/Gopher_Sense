// Autogenerated by SensorCannon
// Hardware config for VCU

#include "dam_hw_config.h"
#include "gopher_sense.h"
#include "GopherCAN.h"
#include "DAM.h"

// BUCKETS
// setup the param lists for each bucket

GENERAL_PARAMETER bucket_1_bucket_general_param_list[2] =
{
  {
    .can_param = (CAN_INFO_STRUCT*)(&vcu_apps2),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&vcu_brake_pressure_front),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  
};

GENERAL_PARAMETER bucket_2_bucket_general_param_list[63] =
{
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_igbt_a),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_igbt_b),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_igbt_c),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_gate_driver),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_ctrl_board),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_rtd_1),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_rtd_2),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_rtd_3),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_rtd_4),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_temp_rtd_5),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_motor_temp),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_torque_shudder),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_digital_in_1),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_digital_in_2),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_digital_in_3),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_digital_in_4),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_digital_in_5),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_digital_in_6),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_digital_in_7),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_digital_in_8),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_motor_angle),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_motor_speed),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_motor_ac_freq),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_delta_resolver_filtered),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_current_phase_a),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_current_phase_b),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_current_phase_c),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_current_dc_bus),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_dc_bus_voltage),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_peak_output_voltage),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_vd_voltage),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_vq_voltage),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_flux_cmd),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_flux_feedback),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_id_feedback),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_iq_feedback),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_1v5_ref),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_2v5_ref),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_5v_ref),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_12v_ref),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_post_fault_lo),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_post_fault_hi),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_run_fault_lo),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_run_fault_hi),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_torque_cmd),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_torque_feedback),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_power_on_timer),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_modulation_index),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_flux_weakening),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_id_cmd),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_iq_cmd),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_torque_cmd_high),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_torque_feedback_high),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_motor_speed_high),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_dc_bus_voltage_high),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_vsm_state),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_pwm_freq),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_inverter_state),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_relay_state),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_states_byte4),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_states_byte5),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_states_byte6),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inv_states_byte7),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  
};


// initalize the bucket structs
BUCKET bucket_list[NUM_BUCKETS] =
{
  
  // Define bucket bucket_1
  {
    .bucket_id = 1,
    .ms_between_req = 20,
    .state = BUCKET_CONFIG_INIT,
    .param_list = 
    {
      .len = 2,
      .list = bucket_1_bucket_general_param_list
    }
  },
  
  // Define bucket bucket_2
  {
    .bucket_id = 2,
    .ms_between_req = 100,
    .state = BUCKET_CONFIG_INIT,
    .param_list = 
    {
      .len = 63,
      .list = bucket_2_bucket_general_param_list
    }
  },
  
};

// ***************************************************************************



// BEGIN ADC1 PARAMS

// give memory to the buffers


#define ADC1_PARAM1_BUF_SIZE 1
U32 adc1_param1_buffer_mem[ADC1_PARAM1_BUF_SIZE];


#define ADC1_PARAM2_BUF_SIZE 1
U32 adc1_param2_buffer_mem[ADC1_PARAM2_BUF_SIZE];

// Fill out the array with all of the sensor params
ANALOG_SENSOR_PARAM adc1_sensor_params[NUM_ADC1_PARAMS] = 
{
  
  
  // adc1_param1
  {
    .bucket_param = bucket_1_bucket_general_param_list + 0,
    .analog_sensor = &linear_position_sensor_25mm,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param1_buffer_mem,
      .buffer_size = ADC1_PARAM1_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM1_BUF_SIZE
  
  
  // adc1_param2
  {
    .bucket_param = bucket_1_bucket_general_param_list + 1,
    .analog_sensor = &brake_pressure_sensor,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param2_buffer_mem,
      .buffer_size = ADC1_PARAM2_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM2_BUF_SIZE
  
};

// ***************************************************************************



// No parameters on ADC2

// ***************************************************************************



// No parameters on ADC3

// ***************************************************************************



// BEGIN CAN PARAMS
// give memory to the buffers


#define CAN_PARAM1_BUF_SIZE 1
U32 can_param1_buffer_mem[CAN_PARAM1_BUF_SIZE];


#define CAN_PARAM2_BUF_SIZE 1
U32 can_param2_buffer_mem[CAN_PARAM2_BUF_SIZE];


#define CAN_PARAM3_BUF_SIZE 1
U32 can_param3_buffer_mem[CAN_PARAM3_BUF_SIZE];


#define CAN_PARAM4_BUF_SIZE 1
U32 can_param4_buffer_mem[CAN_PARAM4_BUF_SIZE];


#define CAN_PARAM5_BUF_SIZE 1
U32 can_param5_buffer_mem[CAN_PARAM5_BUF_SIZE];


#define CAN_PARAM6_BUF_SIZE 1
U32 can_param6_buffer_mem[CAN_PARAM6_BUF_SIZE];


#define CAN_PARAM7_BUF_SIZE 1
U32 can_param7_buffer_mem[CAN_PARAM7_BUF_SIZE];


#define CAN_PARAM8_BUF_SIZE 1
U32 can_param8_buffer_mem[CAN_PARAM8_BUF_SIZE];


#define CAN_PARAM9_BUF_SIZE 1
U32 can_param9_buffer_mem[CAN_PARAM9_BUF_SIZE];


#define CAN_PARAM10_BUF_SIZE 1
U32 can_param10_buffer_mem[CAN_PARAM10_BUF_SIZE];


#define CAN_PARAM11_BUF_SIZE 1
U32 can_param11_buffer_mem[CAN_PARAM11_BUF_SIZE];


#define CAN_PARAM12_BUF_SIZE 1
U32 can_param12_buffer_mem[CAN_PARAM12_BUF_SIZE];


#define CAN_PARAM13_BUF_SIZE 1
U32 can_param13_buffer_mem[CAN_PARAM13_BUF_SIZE];


#define CAN_PARAM14_BUF_SIZE 1
U32 can_param14_buffer_mem[CAN_PARAM14_BUF_SIZE];


#define CAN_PARAM15_BUF_SIZE 1
U32 can_param15_buffer_mem[CAN_PARAM15_BUF_SIZE];


#define CAN_PARAM16_BUF_SIZE 1
U32 can_param16_buffer_mem[CAN_PARAM16_BUF_SIZE];


#define CAN_PARAM17_BUF_SIZE 1
U32 can_param17_buffer_mem[CAN_PARAM17_BUF_SIZE];


#define CAN_PARAM18_BUF_SIZE 1
U32 can_param18_buffer_mem[CAN_PARAM18_BUF_SIZE];


#define CAN_PARAM19_BUF_SIZE 1
U32 can_param19_buffer_mem[CAN_PARAM19_BUF_SIZE];


#define CAN_PARAM20_BUF_SIZE 1
U32 can_param20_buffer_mem[CAN_PARAM20_BUF_SIZE];


#define CAN_PARAM21_BUF_SIZE 1
U32 can_param21_buffer_mem[CAN_PARAM21_BUF_SIZE];


#define CAN_PARAM22_BUF_SIZE 1
U32 can_param22_buffer_mem[CAN_PARAM22_BUF_SIZE];


#define CAN_PARAM23_BUF_SIZE 1
U32 can_param23_buffer_mem[CAN_PARAM23_BUF_SIZE];


#define CAN_PARAM24_BUF_SIZE 1
U32 can_param24_buffer_mem[CAN_PARAM24_BUF_SIZE];


#define CAN_PARAM25_BUF_SIZE 1
U32 can_param25_buffer_mem[CAN_PARAM25_BUF_SIZE];


#define CAN_PARAM26_BUF_SIZE 1
U32 can_param26_buffer_mem[CAN_PARAM26_BUF_SIZE];


#define CAN_PARAM27_BUF_SIZE 1
U32 can_param27_buffer_mem[CAN_PARAM27_BUF_SIZE];


#define CAN_PARAM28_BUF_SIZE 1
U32 can_param28_buffer_mem[CAN_PARAM28_BUF_SIZE];


#define CAN_PARAM29_BUF_SIZE 1
U32 can_param29_buffer_mem[CAN_PARAM29_BUF_SIZE];


#define CAN_PARAM30_BUF_SIZE 1
U32 can_param30_buffer_mem[CAN_PARAM30_BUF_SIZE];


#define CAN_PARAM31_BUF_SIZE 1
U32 can_param31_buffer_mem[CAN_PARAM31_BUF_SIZE];


#define CAN_PARAM32_BUF_SIZE 1
U32 can_param32_buffer_mem[CAN_PARAM32_BUF_SIZE];


#define CAN_PARAM33_BUF_SIZE 1
U32 can_param33_buffer_mem[CAN_PARAM33_BUF_SIZE];


#define CAN_PARAM34_BUF_SIZE 1
U32 can_param34_buffer_mem[CAN_PARAM34_BUF_SIZE];


#define CAN_PARAM35_BUF_SIZE 1
U32 can_param35_buffer_mem[CAN_PARAM35_BUF_SIZE];


#define CAN_PARAM36_BUF_SIZE 1
U32 can_param36_buffer_mem[CAN_PARAM36_BUF_SIZE];


#define CAN_PARAM37_BUF_SIZE 1
U32 can_param37_buffer_mem[CAN_PARAM37_BUF_SIZE];


#define CAN_PARAM38_BUF_SIZE 1
U32 can_param38_buffer_mem[CAN_PARAM38_BUF_SIZE];


#define CAN_PARAM39_BUF_SIZE 1
U32 can_param39_buffer_mem[CAN_PARAM39_BUF_SIZE];


#define CAN_PARAM40_BUF_SIZE 1
U32 can_param40_buffer_mem[CAN_PARAM40_BUF_SIZE];


#define CAN_PARAM41_BUF_SIZE 1
U32 can_param41_buffer_mem[CAN_PARAM41_BUF_SIZE];


#define CAN_PARAM42_BUF_SIZE 1
U32 can_param42_buffer_mem[CAN_PARAM42_BUF_SIZE];


#define CAN_PARAM43_BUF_SIZE 1
U32 can_param43_buffer_mem[CAN_PARAM43_BUF_SIZE];


#define CAN_PARAM44_BUF_SIZE 1
U32 can_param44_buffer_mem[CAN_PARAM44_BUF_SIZE];


#define CAN_PARAM45_BUF_SIZE 1
U32 can_param45_buffer_mem[CAN_PARAM45_BUF_SIZE];


#define CAN_PARAM46_BUF_SIZE 1
U32 can_param46_buffer_mem[CAN_PARAM46_BUF_SIZE];


#define CAN_PARAM47_BUF_SIZE 1
U32 can_param47_buffer_mem[CAN_PARAM47_BUF_SIZE];


#define CAN_PARAM48_BUF_SIZE 1
U32 can_param48_buffer_mem[CAN_PARAM48_BUF_SIZE];


#define CAN_PARAM49_BUF_SIZE 1
U32 can_param49_buffer_mem[CAN_PARAM49_BUF_SIZE];


#define CAN_PARAM50_BUF_SIZE 1
U32 can_param50_buffer_mem[CAN_PARAM50_BUF_SIZE];


#define CAN_PARAM51_BUF_SIZE 1
U32 can_param51_buffer_mem[CAN_PARAM51_BUF_SIZE];


#define CAN_PARAM52_BUF_SIZE 1
U32 can_param52_buffer_mem[CAN_PARAM52_BUF_SIZE];


#define CAN_PARAM53_BUF_SIZE 1
U32 can_param53_buffer_mem[CAN_PARAM53_BUF_SIZE];


#define CAN_PARAM54_BUF_SIZE 1
U32 can_param54_buffer_mem[CAN_PARAM54_BUF_SIZE];


#define CAN_PARAM55_BUF_SIZE 1
U32 can_param55_buffer_mem[CAN_PARAM55_BUF_SIZE];


#define CAN_PARAM56_BUF_SIZE 1
U32 can_param56_buffer_mem[CAN_PARAM56_BUF_SIZE];


#define CAN_PARAM57_BUF_SIZE 1
U32 can_param57_buffer_mem[CAN_PARAM57_BUF_SIZE];


#define CAN_PARAM58_BUF_SIZE 1
U32 can_param58_buffer_mem[CAN_PARAM58_BUF_SIZE];


#define CAN_PARAM59_BUF_SIZE 1
U32 can_param59_buffer_mem[CAN_PARAM59_BUF_SIZE];


#define CAN_PARAM60_BUF_SIZE 1
U32 can_param60_buffer_mem[CAN_PARAM60_BUF_SIZE];


#define CAN_PARAM61_BUF_SIZE 1
U32 can_param61_buffer_mem[CAN_PARAM61_BUF_SIZE];


#define CAN_PARAM62_BUF_SIZE 1
U32 can_param62_buffer_mem[CAN_PARAM62_BUF_SIZE];


#define CAN_PARAM63_BUF_SIZE 1
U32 can_param63_buffer_mem[CAN_PARAM63_BUF_SIZE];

// Fill out the array with all of the sensor params
CAN_SENSOR_PARAM can_sensor_params[NUM_CAN_SENSOR_PARAMS] =
{
  
  
  
  // can_param1
  {

    .bucket_param = bucket_2_bucket_general_param_list + 0,
    .can_sensor = &inverter,
    .message_idx = 0,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param1_buffer_mem,
      .buffer_size = CAN_PARAM1_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM1_BUF_SIZE
  
  
  
  // can_param2
  {

    .bucket_param = bucket_2_bucket_general_param_list + 1,
    .can_sensor = &inverter,
    .message_idx = 1,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param2_buffer_mem,
      .buffer_size = CAN_PARAM2_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM2_BUF_SIZE
  
  
  
  // can_param3
  {

    .bucket_param = bucket_2_bucket_general_param_list + 2,
    .can_sensor = &inverter,
    .message_idx = 2,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param3_buffer_mem,
      .buffer_size = CAN_PARAM3_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM3_BUF_SIZE
  
  
  
  // can_param4
  {

    .bucket_param = bucket_2_bucket_general_param_list + 3,
    .can_sensor = &inverter,
    .message_idx = 3,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param4_buffer_mem,
      .buffer_size = CAN_PARAM4_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM4_BUF_SIZE
  
  
  
  // can_param5
  {

    .bucket_param = bucket_2_bucket_general_param_list + 4,
    .can_sensor = &inverter,
    .message_idx = 4,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param5_buffer_mem,
      .buffer_size = CAN_PARAM5_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM5_BUF_SIZE
  
  
  
  // can_param6
  {

    .bucket_param = bucket_2_bucket_general_param_list + 5,
    .can_sensor = &inverter,
    .message_idx = 5,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param6_buffer_mem,
      .buffer_size = CAN_PARAM6_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM6_BUF_SIZE
  
  
  
  // can_param7
  {

    .bucket_param = bucket_2_bucket_general_param_list + 6,
    .can_sensor = &inverter,
    .message_idx = 6,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param7_buffer_mem,
      .buffer_size = CAN_PARAM7_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM7_BUF_SIZE
  
  
  
  // can_param8
  {

    .bucket_param = bucket_2_bucket_general_param_list + 7,
    .can_sensor = &inverter,
    .message_idx = 7,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param8_buffer_mem,
      .buffer_size = CAN_PARAM8_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM8_BUF_SIZE
  
  
  
  // can_param9
  {

    .bucket_param = bucket_2_bucket_general_param_list + 8,
    .can_sensor = &inverter,
    .message_idx = 8,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param9_buffer_mem,
      .buffer_size = CAN_PARAM9_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM9_BUF_SIZE
  
  
  
  // can_param10
  {

    .bucket_param = bucket_2_bucket_general_param_list + 9,
    .can_sensor = &inverter,
    .message_idx = 9,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param10_buffer_mem,
      .buffer_size = CAN_PARAM10_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM10_BUF_SIZE
  
  
  
  // can_param11
  {

    .bucket_param = bucket_2_bucket_general_param_list + 10,
    .can_sensor = &inverter,
    .message_idx = 10,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param11_buffer_mem,
      .buffer_size = CAN_PARAM11_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM11_BUF_SIZE
  
  
  
  // can_param12
  {

    .bucket_param = bucket_2_bucket_general_param_list + 11,
    .can_sensor = &inverter,
    .message_idx = 11,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param12_buffer_mem,
      .buffer_size = CAN_PARAM12_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM12_BUF_SIZE
  
  
  
  // can_param13
  {

    .bucket_param = bucket_2_bucket_general_param_list + 12,
    .can_sensor = &inverter,
    .message_idx = 12,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param13_buffer_mem,
      .buffer_size = CAN_PARAM13_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM13_BUF_SIZE
  
  
  
  // can_param14
  {

    .bucket_param = bucket_2_bucket_general_param_list + 13,
    .can_sensor = &inverter,
    .message_idx = 13,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param14_buffer_mem,
      .buffer_size = CAN_PARAM14_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM14_BUF_SIZE
  
  
  
  // can_param15
  {

    .bucket_param = bucket_2_bucket_general_param_list + 14,
    .can_sensor = &inverter,
    .message_idx = 14,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param15_buffer_mem,
      .buffer_size = CAN_PARAM15_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM15_BUF_SIZE
  
  
  
  // can_param16
  {

    .bucket_param = bucket_2_bucket_general_param_list + 15,
    .can_sensor = &inverter,
    .message_idx = 15,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param16_buffer_mem,
      .buffer_size = CAN_PARAM16_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM16_BUF_SIZE
  
  
  
  // can_param17
  {

    .bucket_param = bucket_2_bucket_general_param_list + 16,
    .can_sensor = &inverter,
    .message_idx = 16,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param17_buffer_mem,
      .buffer_size = CAN_PARAM17_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM17_BUF_SIZE
  
  
  
  // can_param18
  {

    .bucket_param = bucket_2_bucket_general_param_list + 17,
    .can_sensor = &inverter,
    .message_idx = 17,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param18_buffer_mem,
      .buffer_size = CAN_PARAM18_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM18_BUF_SIZE
  
  
  
  // can_param19
  {

    .bucket_param = bucket_2_bucket_general_param_list + 18,
    .can_sensor = &inverter,
    .message_idx = 18,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param19_buffer_mem,
      .buffer_size = CAN_PARAM19_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM19_BUF_SIZE
  
  
  
  // can_param20
  {

    .bucket_param = bucket_2_bucket_general_param_list + 19,
    .can_sensor = &inverter,
    .message_idx = 19,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param20_buffer_mem,
      .buffer_size = CAN_PARAM20_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM20_BUF_SIZE
  
  
  
  // can_param21
  {

    .bucket_param = bucket_2_bucket_general_param_list + 20,
    .can_sensor = &inverter,
    .message_idx = 20,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param21_buffer_mem,
      .buffer_size = CAN_PARAM21_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM21_BUF_SIZE
  
  
  
  // can_param22
  {

    .bucket_param = bucket_2_bucket_general_param_list + 21,
    .can_sensor = &inverter,
    .message_idx = 21,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param22_buffer_mem,
      .buffer_size = CAN_PARAM22_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM22_BUF_SIZE
  
  
  
  // can_param23
  {

    .bucket_param = bucket_2_bucket_general_param_list + 22,
    .can_sensor = &inverter,
    .message_idx = 22,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param23_buffer_mem,
      .buffer_size = CAN_PARAM23_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM23_BUF_SIZE
  
  
  
  // can_param24
  {

    .bucket_param = bucket_2_bucket_general_param_list + 23,
    .can_sensor = &inverter,
    .message_idx = 23,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param24_buffer_mem,
      .buffer_size = CAN_PARAM24_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM24_BUF_SIZE
  
  
  
  // can_param25
  {

    .bucket_param = bucket_2_bucket_general_param_list + 24,
    .can_sensor = &inverter,
    .message_idx = 24,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param25_buffer_mem,
      .buffer_size = CAN_PARAM25_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM25_BUF_SIZE
  
  
  
  // can_param26
  {

    .bucket_param = bucket_2_bucket_general_param_list + 25,
    .can_sensor = &inverter,
    .message_idx = 25,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param26_buffer_mem,
      .buffer_size = CAN_PARAM26_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM26_BUF_SIZE
  
  
  
  // can_param27
  {

    .bucket_param = bucket_2_bucket_general_param_list + 26,
    .can_sensor = &inverter,
    .message_idx = 26,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param27_buffer_mem,
      .buffer_size = CAN_PARAM27_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM27_BUF_SIZE
  
  
  
  // can_param28
  {

    .bucket_param = bucket_2_bucket_general_param_list + 27,
    .can_sensor = &inverter,
    .message_idx = 27,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param28_buffer_mem,
      .buffer_size = CAN_PARAM28_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM28_BUF_SIZE
  
  
  
  // can_param29
  {

    .bucket_param = bucket_2_bucket_general_param_list + 28,
    .can_sensor = &inverter,
    .message_idx = 28,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param29_buffer_mem,
      .buffer_size = CAN_PARAM29_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM29_BUF_SIZE
  
  
  
  // can_param30
  {

    .bucket_param = bucket_2_bucket_general_param_list + 29,
    .can_sensor = &inverter,
    .message_idx = 29,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param30_buffer_mem,
      .buffer_size = CAN_PARAM30_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM30_BUF_SIZE
  
  
  
  // can_param31
  {

    .bucket_param = bucket_2_bucket_general_param_list + 30,
    .can_sensor = &inverter,
    .message_idx = 30,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param31_buffer_mem,
      .buffer_size = CAN_PARAM31_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM31_BUF_SIZE
  
  
  
  // can_param32
  {

    .bucket_param = bucket_2_bucket_general_param_list + 31,
    .can_sensor = &inverter,
    .message_idx = 31,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param32_buffer_mem,
      .buffer_size = CAN_PARAM32_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM32_BUF_SIZE
  
  
  
  // can_param33
  {

    .bucket_param = bucket_2_bucket_general_param_list + 32,
    .can_sensor = &inverter,
    .message_idx = 32,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param33_buffer_mem,
      .buffer_size = CAN_PARAM33_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM33_BUF_SIZE
  
  
  
  // can_param34
  {

    .bucket_param = bucket_2_bucket_general_param_list + 33,
    .can_sensor = &inverter,
    .message_idx = 33,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param34_buffer_mem,
      .buffer_size = CAN_PARAM34_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM34_BUF_SIZE
  
  
  
  // can_param35
  {

    .bucket_param = bucket_2_bucket_general_param_list + 34,
    .can_sensor = &inverter,
    .message_idx = 34,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param35_buffer_mem,
      .buffer_size = CAN_PARAM35_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM35_BUF_SIZE
  
  
  
  // can_param36
  {

    .bucket_param = bucket_2_bucket_general_param_list + 35,
    .can_sensor = &inverter,
    .message_idx = 35,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param36_buffer_mem,
      .buffer_size = CAN_PARAM36_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM36_BUF_SIZE
  
  
  
  // can_param37
  {

    .bucket_param = bucket_2_bucket_general_param_list + 36,
    .can_sensor = &inverter,
    .message_idx = 36,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param37_buffer_mem,
      .buffer_size = CAN_PARAM37_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM37_BUF_SIZE
  
  
  
  // can_param38
  {

    .bucket_param = bucket_2_bucket_general_param_list + 37,
    .can_sensor = &inverter,
    .message_idx = 37,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param38_buffer_mem,
      .buffer_size = CAN_PARAM38_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM38_BUF_SIZE
  
  
  
  // can_param39
  {

    .bucket_param = bucket_2_bucket_general_param_list + 38,
    .can_sensor = &inverter,
    .message_idx = 38,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param39_buffer_mem,
      .buffer_size = CAN_PARAM39_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM39_BUF_SIZE
  
  
  
  // can_param40
  {

    .bucket_param = bucket_2_bucket_general_param_list + 39,
    .can_sensor = &inverter,
    .message_idx = 39,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param40_buffer_mem,
      .buffer_size = CAN_PARAM40_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM40_BUF_SIZE
  
  
  
  // can_param41
  {

    .bucket_param = bucket_2_bucket_general_param_list + 40,
    .can_sensor = &inverter,
    .message_idx = 40,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param41_buffer_mem,
      .buffer_size = CAN_PARAM41_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM41_BUF_SIZE
  
  
  
  // can_param42
  {

    .bucket_param = bucket_2_bucket_general_param_list + 41,
    .can_sensor = &inverter,
    .message_idx = 41,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param42_buffer_mem,
      .buffer_size = CAN_PARAM42_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM42_BUF_SIZE
  
  
  
  // can_param43
  {

    .bucket_param = bucket_2_bucket_general_param_list + 42,
    .can_sensor = &inverter,
    .message_idx = 42,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param43_buffer_mem,
      .buffer_size = CAN_PARAM43_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM43_BUF_SIZE
  
  
  
  // can_param44
  {

    .bucket_param = bucket_2_bucket_general_param_list + 43,
    .can_sensor = &inverter,
    .message_idx = 43,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param44_buffer_mem,
      .buffer_size = CAN_PARAM44_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM44_BUF_SIZE
  
  
  
  // can_param45
  {

    .bucket_param = bucket_2_bucket_general_param_list + 44,
    .can_sensor = &inverter,
    .message_idx = 44,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param45_buffer_mem,
      .buffer_size = CAN_PARAM45_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM45_BUF_SIZE
  
  
  
  // can_param46
  {

    .bucket_param = bucket_2_bucket_general_param_list + 45,
    .can_sensor = &inverter,
    .message_idx = 45,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param46_buffer_mem,
      .buffer_size = CAN_PARAM46_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM46_BUF_SIZE
  
  
  
  // can_param47
  {

    .bucket_param = bucket_2_bucket_general_param_list + 46,
    .can_sensor = &inverter,
    .message_idx = 46,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param47_buffer_mem,
      .buffer_size = CAN_PARAM47_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM47_BUF_SIZE
  
  
  
  // can_param48
  {

    .bucket_param = bucket_2_bucket_general_param_list + 47,
    .can_sensor = &inverter,
    .message_idx = 47,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param48_buffer_mem,
      .buffer_size = CAN_PARAM48_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM48_BUF_SIZE
  
  
  
  // can_param49
  {

    .bucket_param = bucket_2_bucket_general_param_list + 48,
    .can_sensor = &inverter,
    .message_idx = 48,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param49_buffer_mem,
      .buffer_size = CAN_PARAM49_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM49_BUF_SIZE
  
  
  
  // can_param50
  {

    .bucket_param = bucket_2_bucket_general_param_list + 49,
    .can_sensor = &inverter,
    .message_idx = 49,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param50_buffer_mem,
      .buffer_size = CAN_PARAM50_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM50_BUF_SIZE
  
  
  
  // can_param51
  {

    .bucket_param = bucket_2_bucket_general_param_list + 50,
    .can_sensor = &inverter,
    .message_idx = 50,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param51_buffer_mem,
      .buffer_size = CAN_PARAM51_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM51_BUF_SIZE
  
  
  
  // can_param52
  {

    .bucket_param = bucket_2_bucket_general_param_list + 51,
    .can_sensor = &inverter,
    .message_idx = 51,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param52_buffer_mem,
      .buffer_size = CAN_PARAM52_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM52_BUF_SIZE
  
  
  
  // can_param53
  {

    .bucket_param = bucket_2_bucket_general_param_list + 52,
    .can_sensor = &inverter,
    .message_idx = 52,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param53_buffer_mem,
      .buffer_size = CAN_PARAM53_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM53_BUF_SIZE
  
  
  
  // can_param54
  {

    .bucket_param = bucket_2_bucket_general_param_list + 53,
    .can_sensor = &inverter,
    .message_idx = 53,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param54_buffer_mem,
      .buffer_size = CAN_PARAM54_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM54_BUF_SIZE
  
  
  
  // can_param55
  {

    .bucket_param = bucket_2_bucket_general_param_list + 54,
    .can_sensor = &inverter,
    .message_idx = 54,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param55_buffer_mem,
      .buffer_size = CAN_PARAM55_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM55_BUF_SIZE
  
  
  
  // can_param56
  {

    .bucket_param = bucket_2_bucket_general_param_list + 55,
    .can_sensor = &inverter,
    .message_idx = 55,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param56_buffer_mem,
      .buffer_size = CAN_PARAM56_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM56_BUF_SIZE
  
  
  
  // can_param57
  {

    .bucket_param = bucket_2_bucket_general_param_list + 56,
    .can_sensor = &inverter,
    .message_idx = 56,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param57_buffer_mem,
      .buffer_size = CAN_PARAM57_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM57_BUF_SIZE
  
  
  
  // can_param58
  {

    .bucket_param = bucket_2_bucket_general_param_list + 57,
    .can_sensor = &inverter,
    .message_idx = 57,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param58_buffer_mem,
      .buffer_size = CAN_PARAM58_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM58_BUF_SIZE
  
  
  
  // can_param59
  {

    .bucket_param = bucket_2_bucket_general_param_list + 58,
    .can_sensor = &inverter,
    .message_idx = 58,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param59_buffer_mem,
      .buffer_size = CAN_PARAM59_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM59_BUF_SIZE
  
  
  
  // can_param60
  {

    .bucket_param = bucket_2_bucket_general_param_list + 59,
    .can_sensor = &inverter,
    .message_idx = 59,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param60_buffer_mem,
      .buffer_size = CAN_PARAM60_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM60_BUF_SIZE
  
  
  
  // can_param61
  {

    .bucket_param = bucket_2_bucket_general_param_list + 60,
    .can_sensor = &inverter,
    .message_idx = 60,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param61_buffer_mem,
      .buffer_size = CAN_PARAM61_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM61_BUF_SIZE
  
  
  
  // can_param62
  {

    .bucket_param = bucket_2_bucket_general_param_list + 61,
    .can_sensor = &inverter,
    .message_idx = 61,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param62_buffer_mem,
      .buffer_size = CAN_PARAM62_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM62_BUF_SIZE
  
  
  
  // can_param63
  {

    .bucket_param = bucket_2_bucket_general_param_list + 62,
    .can_sensor = &inverter,
    .message_idx = 62,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param63_buffer_mem,
      .buffer_size = CAN_PARAM63_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM63_BUF_SIZE
  
};

// ***************************************************************************

//END autogenerted file