// Autogenerated by SensorCannon
// Hardware config for TCM_with_ECU_Decoding

#include "dam_hw_config.h"
#include "gopher_sense.h"
#include "GopherCAN.h"
#include "DAM.h"

// BUCKETS
// setup the param lists for each bucket

GENERAL_PARAMETER high_frequency_ecu_bucket_general_param_list[9] =
{
  {
    .can_param = (CAN_INFO_STRUCT*)(&rpm_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&wheel_slip_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&wheel_slip_target_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&aim_rpm_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&wsfl_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&wsfr_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&wsrl_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&wsrr_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&ign_cut_level_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  
};

GENERAL_PARAMETER low_frequency_ecu_bucket_general_param_list[26] =
{
  {
    .can_param = (CAN_INFO_STRUCT*)(&bat_volts_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&engine_temp_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&inlet_air_temp_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&lambda_1_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&throttle_pos_1_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&throttle_pos_2_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pedal_pos_1_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pedal_pos_2_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&man_pressure_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&fuel_cut_lvl_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&ign_advance_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&oil_pressure_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&oil_temp_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&fuel_pressure_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&throttle_pos_required_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&brake_pressure_front_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&etc_duty_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&fan_duty_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&fanoil_duty_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&wpump_duty_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&brake_light_duty_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&warning_light_duty_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&aux_5v_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&eng_5v_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&gps_lat_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&gps_long_ecu),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  
};

GENERAL_PARAMETER high_frequency_tcm_bucket_general_param_list[2] =
{
  {
    .can_param = (CAN_INFO_STRUCT*)(&rear_left_shock_pot),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&rear_right_shock_pot),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  
};

GENERAL_PARAMETER low_frequency_tcm_bucket_general_param_list[19] =
{
  {
    .can_param = (CAN_INFO_STRUCT*)(&sw_upshift),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&sw_downshift),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&sw_clutch_fast),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&sw_clutch_slow),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&sw_aero_front),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&sw_aero_rear),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_neutral),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_target_rpm),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_current_gear),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_target_gear),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_currently_moving),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_successful_shift),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_shifter_position),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_clutch_position),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_trans_rpm),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_throttle_blip),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_using_clutch),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_anti_stall),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&tcm_lap_timer),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  
};


// initalize the bucket structs
BUCKET bucket_list[NUM_BUCKETS] =
{
  
  // Define bucket high_frequency_ecu
  {
    .bucket_id = 1,
    .ms_between_req = 5,
    .state = BUCKET_CONFIG_INIT,
    .param_list = 
    {
      .len = 9,
      .list = high_frequency_ecu_bucket_general_param_list
    }
  },
  
  // Define bucket low_frequency_ecu
  {
    .bucket_id = 2,
    .ms_between_req = 50,
    .state = BUCKET_CONFIG_INIT,
    .param_list = 
    {
      .len = 26,
      .list = low_frequency_ecu_bucket_general_param_list
    }
  },
  
  // Define bucket high_frequency_tcm
  {
    .bucket_id = 3,
    .ms_between_req = 4,
    .state = BUCKET_CONFIG_INIT,
    .param_list = 
    {
      .len = 2,
      .list = high_frequency_tcm_bucket_general_param_list
    }
  },
  
  // Define bucket low_frequency_tcm
  {
    .bucket_id = 4,
    .ms_between_req = 10,
    .state = BUCKET_CONFIG_INIT,
    .param_list = 
    {
      .len = 19,
      .list = low_frequency_tcm_bucket_general_param_list
    }
  },
  
};

// ***************************************************************************



// BEGIN ADC1 PARAMS

// give memory to the buffers


#define ADC1_PARAM1_BUF_SIZE 1
U32 adc1_param1_buffer_mem[ADC1_PARAM1_BUF_SIZE];


#define ADC1_PARAM2_BUF_SIZE 1
U32 adc1_param2_buffer_mem[ADC1_PARAM2_BUF_SIZE];


#define ADC1_PARAM3_BUF_SIZE 1
U32 adc1_param3_buffer_mem[ADC1_PARAM3_BUF_SIZE];

// Fill out the array with all of the sensor params
ANALOG_SENSOR_PARAM adc1_sensor_params[NUM_ADC1_PARAMS] = 
{
  
  
  // adc1_param1
  {
    .bucket_param = high_frequency_tcm_bucket_general_param_list + 0,
    .analog_sensor = &linear_position_sensor_25mm,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param1_buffer_mem,
      .buffer_size = ADC1_PARAM1_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM1_BUF_SIZE
  
  
  // adc1_param2
  {
    .bucket_param = high_frequency_tcm_bucket_general_param_list + 1,
    .analog_sensor = &linear_position_sensor_25mm,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param2_buffer_mem,
      .buffer_size = ADC1_PARAM2_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM2_BUF_SIZE
  
  
  // adc1_param3
  {
    .bucket_param = low_frequency_tcm_bucket_general_param_list + 13,
    .analog_sensor = &linear_position_sensor_50mm,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param3_buffer_mem,
      .buffer_size = ADC1_PARAM3_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM3_BUF_SIZE
  
};

// ***************************************************************************



// BEGIN ADC2 PARAMS

// give memory to the buffers


#define ADC2_PARAM1_BUF_SIZE 1
U32 adc2_param1_buffer_mem[ADC2_PARAM1_BUF_SIZE];

// Fill out the array with all of the sensor params
ANALOG_SENSOR_PARAM adc2_sensor_params[NUM_ADC2_PARAMS] = 
{
  
  
  // adc2_param1
  {
    .bucket_param = low_frequency_tcm_bucket_general_param_list + 12,
    .analog_sensor = &linear_position_sensor_50mm,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc2_param1_buffer_mem,
      .buffer_size = ADC2_PARAM1_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC2_PARAM1_BUF_SIZE
  
};

// ***************************************************************************



// No parameters on ADC3

// ***************************************************************************



// BEGIN CAN PARAMS
// give memory to the buffers


#define CAN_PARAM1_BUF_SIZE 1
U32 can_param1_buffer_mem[CAN_PARAM1_BUF_SIZE];


#define CAN_PARAM2_BUF_SIZE 1
U32 can_param2_buffer_mem[CAN_PARAM2_BUF_SIZE];


#define CAN_PARAM3_BUF_SIZE 1
U32 can_param3_buffer_mem[CAN_PARAM3_BUF_SIZE];


#define CAN_PARAM4_BUF_SIZE 1
U32 can_param4_buffer_mem[CAN_PARAM4_BUF_SIZE];


#define CAN_PARAM5_BUF_SIZE 1
U32 can_param5_buffer_mem[CAN_PARAM5_BUF_SIZE];


#define CAN_PARAM6_BUF_SIZE 1
U32 can_param6_buffer_mem[CAN_PARAM6_BUF_SIZE];


#define CAN_PARAM7_BUF_SIZE 1
U32 can_param7_buffer_mem[CAN_PARAM7_BUF_SIZE];


#define CAN_PARAM8_BUF_SIZE 1
U32 can_param8_buffer_mem[CAN_PARAM8_BUF_SIZE];


#define CAN_PARAM9_BUF_SIZE 1
U32 can_param9_buffer_mem[CAN_PARAM9_BUF_SIZE];


#define CAN_PARAM10_BUF_SIZE 1
U32 can_param10_buffer_mem[CAN_PARAM10_BUF_SIZE];


#define CAN_PARAM11_BUF_SIZE 1
U32 can_param11_buffer_mem[CAN_PARAM11_BUF_SIZE];


#define CAN_PARAM12_BUF_SIZE 1
U32 can_param12_buffer_mem[CAN_PARAM12_BUF_SIZE];


#define CAN_PARAM13_BUF_SIZE 1
U32 can_param13_buffer_mem[CAN_PARAM13_BUF_SIZE];


#define CAN_PARAM14_BUF_SIZE 1
U32 can_param14_buffer_mem[CAN_PARAM14_BUF_SIZE];


#define CAN_PARAM15_BUF_SIZE 1
U32 can_param15_buffer_mem[CAN_PARAM15_BUF_SIZE];


#define CAN_PARAM16_BUF_SIZE 1
U32 can_param16_buffer_mem[CAN_PARAM16_BUF_SIZE];


#define CAN_PARAM17_BUF_SIZE 1
U32 can_param17_buffer_mem[CAN_PARAM17_BUF_SIZE];


#define CAN_PARAM18_BUF_SIZE 1
U32 can_param18_buffer_mem[CAN_PARAM18_BUF_SIZE];


#define CAN_PARAM19_BUF_SIZE 1
U32 can_param19_buffer_mem[CAN_PARAM19_BUF_SIZE];


#define CAN_PARAM20_BUF_SIZE 1
U32 can_param20_buffer_mem[CAN_PARAM20_BUF_SIZE];


#define CAN_PARAM21_BUF_SIZE 1
U32 can_param21_buffer_mem[CAN_PARAM21_BUF_SIZE];


#define CAN_PARAM22_BUF_SIZE 1
U32 can_param22_buffer_mem[CAN_PARAM22_BUF_SIZE];


#define CAN_PARAM23_BUF_SIZE 1
U32 can_param23_buffer_mem[CAN_PARAM23_BUF_SIZE];


#define CAN_PARAM24_BUF_SIZE 1
U32 can_param24_buffer_mem[CAN_PARAM24_BUF_SIZE];


#define CAN_PARAM25_BUF_SIZE 1
U32 can_param25_buffer_mem[CAN_PARAM25_BUF_SIZE];


#define CAN_PARAM26_BUF_SIZE 1
U32 can_param26_buffer_mem[CAN_PARAM26_BUF_SIZE];


#define CAN_PARAM27_BUF_SIZE 1
U32 can_param27_buffer_mem[CAN_PARAM27_BUF_SIZE];


#define CAN_PARAM28_BUF_SIZE 1
U32 can_param28_buffer_mem[CAN_PARAM28_BUF_SIZE];


#define CAN_PARAM29_BUF_SIZE 1
U32 can_param29_buffer_mem[CAN_PARAM29_BUF_SIZE];


#define CAN_PARAM30_BUF_SIZE 1
U32 can_param30_buffer_mem[CAN_PARAM30_BUF_SIZE];


#define CAN_PARAM31_BUF_SIZE 1
U32 can_param31_buffer_mem[CAN_PARAM31_BUF_SIZE];


#define CAN_PARAM32_BUF_SIZE 1
U32 can_param32_buffer_mem[CAN_PARAM32_BUF_SIZE];


#define CAN_PARAM33_BUF_SIZE 1
U32 can_param33_buffer_mem[CAN_PARAM33_BUF_SIZE];


#define CAN_PARAM34_BUF_SIZE 1
U32 can_param34_buffer_mem[CAN_PARAM34_BUF_SIZE];


#define CAN_PARAM35_BUF_SIZE 1
U32 can_param35_buffer_mem[CAN_PARAM35_BUF_SIZE];

// Fill out the array with all of the sensor params
CAN_SENSOR_PARAM can_sensor_params[NUM_CAN_SENSOR_PARAMS] =
{
  
  
  
  // can_param1
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 0,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 0,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param1_buffer_mem,
      .buffer_size = CAN_PARAM1_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM1_BUF_SIZE
  
  
  
  // can_param2
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 1,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 1,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param2_buffer_mem,
      .buffer_size = CAN_PARAM2_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM2_BUF_SIZE
  
  
  
  // can_param3
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 2,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 2,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param3_buffer_mem,
      .buffer_size = CAN_PARAM3_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM3_BUF_SIZE
  
  
  
  // can_param4
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 3,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 3,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param4_buffer_mem,
      .buffer_size = CAN_PARAM4_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM4_BUF_SIZE
  
  
  
  // can_param5
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 4,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 4,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param5_buffer_mem,
      .buffer_size = CAN_PARAM5_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM5_BUF_SIZE
  
  
  
  // can_param6
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 5,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 5,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param6_buffer_mem,
      .buffer_size = CAN_PARAM6_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM6_BUF_SIZE
  
  
  
  // can_param7
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 6,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 6,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param7_buffer_mem,
      .buffer_size = CAN_PARAM7_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM7_BUF_SIZE
  
  
  
  // can_param8
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 7,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 7,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param8_buffer_mem,
      .buffer_size = CAN_PARAM8_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM8_BUF_SIZE
  
  
  
  // can_param9
  {

    .bucket_param = high_frequency_ecu_bucket_general_param_list + 8,
    .can_sensor = &m600_ecu_high_prio_data,
    .message_idx = 8,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param9_buffer_mem,
      .buffer_size = CAN_PARAM9_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM9_BUF_SIZE
  
  
  
  // can_param10
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 0,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 0,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param10_buffer_mem,
      .buffer_size = CAN_PARAM10_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM10_BUF_SIZE
  
  
  
  // can_param11
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 1,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 1,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param11_buffer_mem,
      .buffer_size = CAN_PARAM11_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM11_BUF_SIZE
  
  
  
  // can_param12
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 2,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 2,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param12_buffer_mem,
      .buffer_size = CAN_PARAM12_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM12_BUF_SIZE
  
  
  
  // can_param13
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 3,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 3,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param13_buffer_mem,
      .buffer_size = CAN_PARAM13_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM13_BUF_SIZE
  
  
  
  // can_param14
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 4,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 4,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param14_buffer_mem,
      .buffer_size = CAN_PARAM14_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM14_BUF_SIZE
  
  
  
  // can_param15
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 5,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 5,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param15_buffer_mem,
      .buffer_size = CAN_PARAM15_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM15_BUF_SIZE
  
  
  
  // can_param16
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 6,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 6,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param16_buffer_mem,
      .buffer_size = CAN_PARAM16_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM16_BUF_SIZE
  
  
  
  // can_param17
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 7,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 7,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param17_buffer_mem,
      .buffer_size = CAN_PARAM17_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM17_BUF_SIZE
  
  
  
  // can_param18
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 8,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 8,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param18_buffer_mem,
      .buffer_size = CAN_PARAM18_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM18_BUF_SIZE
  
  
  
  // can_param19
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 9,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 9,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param19_buffer_mem,
      .buffer_size = CAN_PARAM19_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM19_BUF_SIZE
  
  
  
  // can_param20
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 10,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 10,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param20_buffer_mem,
      .buffer_size = CAN_PARAM20_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM20_BUF_SIZE
  
  
  
  // can_param21
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 11,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 11,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param21_buffer_mem,
      .buffer_size = CAN_PARAM21_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM21_BUF_SIZE
  
  
  
  // can_param22
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 12,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 12,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param22_buffer_mem,
      .buffer_size = CAN_PARAM22_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM22_BUF_SIZE
  
  
  
  // can_param23
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 13,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 13,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param23_buffer_mem,
      .buffer_size = CAN_PARAM23_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM23_BUF_SIZE
  
  
  
  // can_param24
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 14,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 14,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param24_buffer_mem,
      .buffer_size = CAN_PARAM24_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM24_BUF_SIZE
  
  
  
  // can_param25
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 15,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 15,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param25_buffer_mem,
      .buffer_size = CAN_PARAM25_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM25_BUF_SIZE
  
  
  
  // can_param26
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 16,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 16,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param26_buffer_mem,
      .buffer_size = CAN_PARAM26_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM26_BUF_SIZE
  
  
  
  // can_param27
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 17,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 17,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param27_buffer_mem,
      .buffer_size = CAN_PARAM27_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM27_BUF_SIZE
  
  
  
  // can_param28
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 18,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 18,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param28_buffer_mem,
      .buffer_size = CAN_PARAM28_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM28_BUF_SIZE
  
  
  
  // can_param29
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 19,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 19,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param29_buffer_mem,
      .buffer_size = CAN_PARAM29_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM29_BUF_SIZE
  
  
  
  // can_param30
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 20,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 20,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param30_buffer_mem,
      .buffer_size = CAN_PARAM30_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM30_BUF_SIZE
  
  
  
  // can_param31
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 21,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 21,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param31_buffer_mem,
      .buffer_size = CAN_PARAM31_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM31_BUF_SIZE
  
  
  
  // can_param32
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 22,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 22,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param32_buffer_mem,
      .buffer_size = CAN_PARAM32_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM32_BUF_SIZE
  
  
  
  // can_param33
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 23,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 23,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param33_buffer_mem,
      .buffer_size = CAN_PARAM33_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM33_BUF_SIZE
  
  
  
  // can_param34
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 24,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 24,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param34_buffer_mem,
      .buffer_size = CAN_PARAM34_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM34_BUF_SIZE
  
  
  
  // can_param35
  {

    .bucket_param = low_frequency_ecu_bucket_general_param_list + 25,
    .can_sensor = &m600_ecu_low_prio_data,
    .message_idx = 25,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = can_param35_buffer_mem,
      .buffer_size = CAN_PARAM35_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    },
	.new_buf_data = FALSE

  },
  #undef CAN_PARAM35_BUF_SIZE
  
};

// ***************************************************************************

//END autogenerted file