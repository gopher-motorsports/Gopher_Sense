// Autogenerated by SensorCannon
// Hardware config for PDM

#include "dam_hw_config.h"
#include "gopher_sense.h"
#include "GopherCAN.h"
#include "DAM.h"

// BUCKETS
// setup the param lists for each bucket

GENERAL_PARAMETER currents_bucket_general_param_list[20] =
{
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_0),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_1),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_2),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_3),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_4),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_5),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_6),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_7),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_8),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_9),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_10),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_11),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_12),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_13),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_14),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_15),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_16),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_17),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_18),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  {
    .can_param = (CAN_INFO_STRUCT*)(&pdm_current_19),
    .status = CLEAN,
	.last_tx = (U32)(-1*MAX_TIME_BETWEEN_TX_ms)
  },
  
};


// initalize the bucket structs
BUCKET bucket_list[NUM_BUCKETS] =
{
  
  // Define bucket currents
  {
    .bucket_id = 1,
    .ms_between_req = 1000,
    .state = BUCKET_CONFIG_INIT,
    .param_list = 
    {
      .len = 20,
      .list = currents_bucket_general_param_list
    }
  },
  
};

// ***************************************************************************



// BEGIN ADC1 PARAMS

// give memory to the buffers


#define ADC1_PARAM1_BUF_SIZE 100
U32 adc1_param1_buffer_mem[ADC1_PARAM1_BUF_SIZE];


#define ADC1_PARAM2_BUF_SIZE 100
U32 adc1_param2_buffer_mem[ADC1_PARAM2_BUF_SIZE];


#define ADC1_PARAM3_BUF_SIZE 100
U32 adc1_param3_buffer_mem[ADC1_PARAM3_BUF_SIZE];


#define ADC1_PARAM4_BUF_SIZE 100
U32 adc1_param4_buffer_mem[ADC1_PARAM4_BUF_SIZE];


#define ADC1_PARAM5_BUF_SIZE 100
U32 adc1_param5_buffer_mem[ADC1_PARAM5_BUF_SIZE];


#define ADC1_PARAM6_BUF_SIZE 100
U32 adc1_param6_buffer_mem[ADC1_PARAM6_BUF_SIZE];


#define ADC1_PARAM7_BUF_SIZE 100
U32 adc1_param7_buffer_mem[ADC1_PARAM7_BUF_SIZE];


#define ADC1_PARAM8_BUF_SIZE 100
U32 adc1_param8_buffer_mem[ADC1_PARAM8_BUF_SIZE];


#define ADC1_PARAM9_BUF_SIZE 100
U32 adc1_param9_buffer_mem[ADC1_PARAM9_BUF_SIZE];


#define ADC1_PARAM10_BUF_SIZE 100
U32 adc1_param10_buffer_mem[ADC1_PARAM10_BUF_SIZE];

// Fill out the array with all of the sensor params
ANALOG_SENSOR_PARAM adc1_sensor_params[NUM_ADC1_PARAMS] = 
{
  
  
  // adc1_param1
  {
    .bucket_param = currents_bucket_general_param_list + 0,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param1_buffer_mem,
      .buffer_size = ADC1_PARAM1_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM1_BUF_SIZE
  
  
  // adc1_param2
  {
    .bucket_param = currents_bucket_general_param_list + 1,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param2_buffer_mem,
      .buffer_size = ADC1_PARAM2_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM2_BUF_SIZE
  
  
  // adc1_param3
  {
    .bucket_param = currents_bucket_general_param_list + 2,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param3_buffer_mem,
      .buffer_size = ADC1_PARAM3_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM3_BUF_SIZE
  
  
  // adc1_param4
  {
    .bucket_param = currents_bucket_general_param_list + 3,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param4_buffer_mem,
      .buffer_size = ADC1_PARAM4_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM4_BUF_SIZE
  
  
  // adc1_param5
  {
    .bucket_param = currents_bucket_general_param_list + 4,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param5_buffer_mem,
      .buffer_size = ADC1_PARAM5_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM5_BUF_SIZE
  
  
  // adc1_param6
  {
    .bucket_param = currents_bucket_general_param_list + 5,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param6_buffer_mem,
      .buffer_size = ADC1_PARAM6_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM6_BUF_SIZE
  
  
  // adc1_param7
  {
    .bucket_param = currents_bucket_general_param_list + 6,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param7_buffer_mem,
      .buffer_size = ADC1_PARAM7_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM7_BUF_SIZE
  
  
  // adc1_param8
  {
    .bucket_param = currents_bucket_general_param_list + 7,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param8_buffer_mem,
      .buffer_size = ADC1_PARAM8_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM8_BUF_SIZE
  
  
  // adc1_param9
  {
    .bucket_param = currents_bucket_general_param_list + 8,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param9_buffer_mem,
      .buffer_size = ADC1_PARAM9_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM9_BUF_SIZE
  
  
  // adc1_param10
  {
    .bucket_param = currents_bucket_general_param_list + 9,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc1_param10_buffer_mem,
      .buffer_size = ADC1_PARAM10_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC1_PARAM10_BUF_SIZE
  
};

// ***************************************************************************



// No parameters on ADC2

// ***************************************************************************



// BEGIN ADC3 PARAMS

// give memory to the buffers


#define ADC3_PARAM1_BUF_SIZE 100
U32 adc3_param1_buffer_mem[ADC3_PARAM1_BUF_SIZE];


#define ADC3_PARAM2_BUF_SIZE 100
U32 adc3_param2_buffer_mem[ADC3_PARAM2_BUF_SIZE];


#define ADC3_PARAM3_BUF_SIZE 100
U32 adc3_param3_buffer_mem[ADC3_PARAM3_BUF_SIZE];


#define ADC3_PARAM4_BUF_SIZE 100
U32 adc3_param4_buffer_mem[ADC3_PARAM4_BUF_SIZE];


#define ADC3_PARAM5_BUF_SIZE 100
U32 adc3_param5_buffer_mem[ADC3_PARAM5_BUF_SIZE];


#define ADC3_PARAM6_BUF_SIZE 100
U32 adc3_param6_buffer_mem[ADC3_PARAM6_BUF_SIZE];


#define ADC3_PARAM7_BUF_SIZE 100
U32 adc3_param7_buffer_mem[ADC3_PARAM7_BUF_SIZE];


#define ADC3_PARAM8_BUF_SIZE 100
U32 adc3_param8_buffer_mem[ADC3_PARAM8_BUF_SIZE];


#define ADC3_PARAM9_BUF_SIZE 100
U32 adc3_param9_buffer_mem[ADC3_PARAM9_BUF_SIZE];


#define ADC3_PARAM10_BUF_SIZE 100
U32 adc3_param10_buffer_mem[ADC3_PARAM10_BUF_SIZE];

// Fill out the array with all of the sensor params
ANALOG_SENSOR_PARAM adc3_sensor_params[NUM_ADC3_PARAMS] = 
{
  
  
  // adc3_param1
  {
    .bucket_param = currents_bucket_general_param_list + 10,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param1_buffer_mem,
      .buffer_size = ADC3_PARAM1_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM1_BUF_SIZE
  
  
  // adc3_param2
  {
    .bucket_param = currents_bucket_general_param_list + 11,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param2_buffer_mem,
      .buffer_size = ADC3_PARAM2_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM2_BUF_SIZE
  
  
  // adc3_param3
  {
    .bucket_param = currents_bucket_general_param_list + 12,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param3_buffer_mem,
      .buffer_size = ADC3_PARAM3_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM3_BUF_SIZE
  
  
  // adc3_param4
  {
    .bucket_param = currents_bucket_general_param_list + 13,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param4_buffer_mem,
      .buffer_size = ADC3_PARAM4_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM4_BUF_SIZE
  
  
  // adc3_param5
  {
    .bucket_param = currents_bucket_general_param_list + 14,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param5_buffer_mem,
      .buffer_size = ADC3_PARAM5_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM5_BUF_SIZE
  
  
  // adc3_param6
  {
    .bucket_param = currents_bucket_general_param_list + 15,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param6_buffer_mem,
      .buffer_size = ADC3_PARAM6_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM6_BUF_SIZE
  
  
  // adc3_param7
  {
    .bucket_param = currents_bucket_general_param_list + 16,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param7_buffer_mem,
      .buffer_size = ADC3_PARAM7_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM7_BUF_SIZE
  
  
  // adc3_param8
  {
    .bucket_param = currents_bucket_general_param_list + 17,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param8_buffer_mem,
      .buffer_size = ADC3_PARAM8_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM8_BUF_SIZE
  
  
  // adc3_param9
  {
    .bucket_param = currents_bucket_general_param_list + 18,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param9_buffer_mem,
      .buffer_size = ADC3_PARAM9_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM9_BUF_SIZE
  
  
  // adc3_param10
  {
    .bucket_param = currents_bucket_general_param_list + 19,
    .analog_sensor = &None,
    .num_filtered_subparams = 0, // not yet implemented
    .filtered_subparams = NULL, // not yet implemented
    .buffer =
    {
      .buffer = adc3_param10_buffer_mem,
      .buffer_size = ADC3_PARAM10_BUF_SIZE,
      .fill_level = 0,
	  .buffer_head = 0
    }
  },
  #undef ADC3_PARAM10_BUF_SIZE
  
};

// ***************************************************************************



// No CAN params

// ***************************************************************************

//END autogenerted file